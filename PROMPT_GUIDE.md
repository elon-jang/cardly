# Cardly 프로젝트 - Claude Code 프롬프트 가이드

이 문서는 Claude Code를 사용하여 Cardly와 같은 프로젝트를 처음부터 만들기 위한 프롬프트 전략을 설명합니다.

## 📋 프롬프트 작성 원칙

### 1. 명확한 목표 설정
- 무엇을 만들 것인지 구체적으로 명시
- 최종 사용자와 사용 사례 정의
- 핵심 가치 제안 설명

### 2. 기술 스택 명시
- 선호하는 프레임워크/라이브러리 지정
- 데이터베이스 및 인증 시스템 선택
- 배포 환경 고려

### 3. 단계별 접근
- 복잡한 기능을 작은 단위로 분해
- MVP(Minimum Viable Product)부터 시작
- 점진적 기능 추가

### 4. 구체적 요구사항
- UI/UX 선호도 명시
- 데이터 구조 설명
- 특정 기능의 동작 방식 설명

## 🎯 단계별 프롬프트 예시

### 1단계: 프로젝트 초기 설정

```
React 19와 Vite를 사용하여 명함 생성 웹 애플리케이션을 만들어줘.

요구사항:
- React 19 + Vite 프로젝트 설정
- Supabase를 백엔드로 사용 (인증 및 데이터베이스)
- 다크 테마 UI
- 반응형 디자인

기술 스택:
- Frontend: React 19, Vite
- Styling: CSS (CSS Variables 사용)
- Backend: Supabase
- Icons: Lucide React

프로젝트 구조를 설정하고 기본 레이아웃을 만들어줘.
```

**예상 결과**: 프로젝트 초기 설정, package.json, 기본 폴더 구조

### 2단계: 인증 시스템

```
Supabase Auth를 사용한 회원가입/로그인 시스템을 구현해줘.

기능:
- 이메일/비밀번호 기반 인증
- 로그인/회원가입 페이지
- 인증 상태 관리 (React Context)
- 로그아웃 기능
- 세션 유지

UI 요구사항:
- 모던하고 깔끔한 폼 디자인
- 로딩 상태 표시
- 에러 메시지 표시
- 입력 유효성 검사
```

**예상 결과**: Login.jsx, Signup.jsx, AuthContext.jsx

### 3단계: 핵심 기능 - 명함 생성

```
명함을 커스터마이징하고 미리보기할 수 있는 기능을 구현해줘.

명함 입력 필드:
- 이름
- 직업/직함
- 전화번호
- 이메일
- Instagram 계정
- 블로그/웹사이트

커스터마이징 옵션:
- 6가지 테마 (Light, Dark, Cream, Navy, Pastel, Earth)
- 6가지 레이아웃 (Standard, Bold, Geometric, Elegant, Creative, Organic)
- 이미지 선택 (프리셋 이미지 제공)
- 이미지 그라디언트 오버레이 옵션

실시간 미리보기:
- 600x350px 크기
- 선택한 테마와 레이아웃이 즉시 반영
- 모바일에서 자동 스케일링
```

**예상 결과**: CardForm.jsx, CardPreview.jsx, 관련 CSS

### 4단계: 데이터베이스 연동

```
Supabase를 사용하여 명함 저장 기능을 구현해줘.

데이터베이스 테이블:
- business_cards 테이블
  - user_id (외래키)
  - name, title, phone, email, instagram, blog
  - image, theme, layout, image_gradient
  - created_at, updated_at

기능:
- 명함 저장 (중복 방지)
- 저장된 명함 목록 조회
- 명함 삭제
- RLS (Row Level Security) 정책 설정

UI:
- 저장 버튼
- 저장 성공/실패 알림
- 저장된 명함 그리드 표시 (2열)
```

**예상 결과**: SQL 스키마, cardService.js, SavedCards.jsx

### 5단계: 사용자 프로필

```
사용자 프로필 관리 기능을 추가해줘.

기능:
- 사용자 개인정보 저장 (이름, 직함, 연락처 등)
- 프로필 정보를 새 명함의 기본값으로 사용
- 프로필 수정 기능

데이터베이스:
- user_profiles 테이블
- user_id와 1:1 관계
- RLS 정책

UI:
- "My Profile" 탭 추가
- 프로필 입력 폼
- 저장 버튼
```

**예상 결과**: UserProfile.jsx, userProfileService.js, SQL 스키마

### 6단계: 커스텀 이미지 업로드

```
사용자가 자신의 이미지를 업로드하고 영구 저장할 수 있는 기능을 추가해줘.

기능:
- 이미지 파일 업로드 (JPG, PNG)
- Supabase에 영구 저장
- 중복 이미지 방지
- 업로드한 이미지를 프리셋 이미지와 함께 표시

데이터베이스:
- custom_images 테이블
- image_data (base64)
- user_id 기준 RLS

UI:
- "Upload Custom Image" 버튼
- 업로드 진행 상태
- 업로드된 이미지 미리보기
```

**예상 결과**: customImageService.js, 업로드 UI, SQL 스키마

### 7단계: 내보내기 기능

```
명함을 PNG와 PDF로 내보낼 수 있는 기능을 구현해줘.

기술 스택:
- html-to-image: PNG 생성
- jsPDF: PDF 생성

기능:
- "Download PNG" 버튼
- "Download PDF" 버튼
- 고품질 이미지 생성 (원본 크기 유지)
- 파일명: {사용자이름}_business_card.{확장자}

구현:
- CardPreview 컴포넌트를 이미지/PDF로 변환
- 다운로드 자동 실행
```

**예상 결과**: export.js, 다운로드 버튼

### 8단계: UX 개선

```
사용자 경험을 개선하기 위한 기능들을 추가해줘.

토스트 알림:
- 성공/에러/정보 메시지
- 자동 닫기 (3초)
- 색상 구분 (녹색/빨강/파랑)

탭 네비게이션:
- Create Card / My Saved Cards / My Profile
- 활성 탭 표시
- 아이콘 + 텍스트

기타 개선:
- 로딩 상태 표시
- 호버 효과
- 부드러운 애니메이션
- 반응형 레이아웃
```

**예상 결과**: Toast.jsx, ToastContext.jsx, CSS 개선

### 9단계: 배포 준비

```
프로젝트를 배포하기 위해 다음을 준비해줘:

1. 문서 작성:
   - README.md: 프로젝트 개요, 기능, 설치 방법
   - SETUP.md: 상세 설정 가이드
   - walkthrough.md: 기능 설명서

2. Git 설정:
   - .gitignore 업데이트
   - Git 저장소 초기화
   - 의미있는 커밋 메시지

3. Vercel 배포:
   - vercel.json 설정 (필요시)
   - 환경 변수 확인
   - 배포 실행
```

**예상 결과**: 문서 파일들, Git 커밋, Vercel 배포

## 💡 효과적인 프롬프트 팁

### 1. 구체적으로 요청하기

**❌ 나쁜 예:**
```
명함 앱 만들어줘
```

**✅ 좋은 예:**
```
React와 Supabase를 사용한 명함 생성 앱을 만들어줘.
사용자가 이름, 직업, 연락처를 입력하고,
여러 테마와 레이아웃 중에서 선택할 수 있어야 해.
명함을 PNG로 다운로드할 수 있어야 하고,
로그인한 사용자는 명함을 저장할 수 있어야 해.
```

### 2. 기술 스택 명시

**✅ 좋은 예:**
```
기술 스택:
- React 19 + Vite
- Supabase (인증 + 데이터베이스)
- CSS Variables (다크 테마)
- Lucide React (아이콘)
- html-to-image, jsPDF (내보내기)
```

### 3. UI/UX 선호도 전달

**✅ 좋은 예:**
```
UI 스타일:
- 모던하고 미니멀한 디자인
- 다크 테마 (배경: #0f172a)
- 둥근 모서리 (border-radius: 12px)
- 부드러운 애니메이션
- 탭 기반 네비게이션
```

### 4. 점진적 개선 요청

**✅ 좋은 예:**
```
명함 저장 기능이 잘 작동하네요.
이제 다음을 추가해줘:
1. 중복 명함 저장 방지
2. 저장 성공 시 토스트 알림 표시
3. 저장된 명함 목록을 2열 그리드로 표시
```

### 5. 문제 발생 시 구체적으로 설명

**✅ 좋은 예:**
```
명함 이미지가 그리드에서 잘려 보여.
이미지가 완전히 보이도록 다음을 수정해줘:
- 컨테이너 높이를 늘리기
- overflow: hidden 제거
- 명함 크기를 70%로 스케일링
```

## 🎓 학습 포인트

### Claude Code와 효과적으로 작업하기

1. **한 번에 하나씩**: 복잡한 기능을 작은 단위로 나누어 요청
2. **확인하며 진행**: 각 단계의 결과를 확인하고 다음으로 진행
3. **구체적인 피드백**: "좋다/나쁘다"보다 "이 부분을 이렇게 바꿔줘"
4. **기술 용어 사용**: React 용어, CSS 속성명 등을 정확히 사용
5. **예시 제공**: 원하는 결과의 예시나 참고 자료 제공

### 프로젝트 진행 순서

1. ✅ 프로젝트 설정 및 기본 구조
2. ✅ 인증 시스템
3. ✅ 핵심 기능 (명함 생성/미리보기)
4. ✅ 데이터 저장 (데이터베이스)
5. ✅ 추가 기능 (프로필, 이미지 업로드)
6. ✅ 내보내기 기능
7. ✅ UX 개선 (토스트, 애니메이션)
8. ✅ 문서화 및 배포

## 📝 실제 프롬프트 예시 (Cardly 개발 과정)

### 시작 프롬프트
```
React 19와 Vite로 명함 생성 웹 애플리케이션을 만들어줘.

목표:
- 음악 강사들이 전문적인 명함을 쉽게 만들 수 있는 도구
- 다양한 테마와 레이아웃 제공
- 온라인 공유 및 인쇄용 다운로드 지원

기술 스택:
- Frontend: React 19 + Vite
- Backend: Supabase (인증, 데이터베이스)
- Styling: CSS (다크 테마)
- Icons: Lucide React

1단계로 프로젝트 설정과 기본 레이아웃을 만들어줘.
```

### 기능 추가 프롬프트
```
명함 입력 폼과 실시간 미리보기 기능을 추가해줘.

입력 필드:
- Full Name
- Profession/Title
- Phone
- Email
- Instagram
- Blog/Website

디자인 옵션:
- Layout: Standard, Elegant, Creative, Organic
- Theme: Light, Dark, Cream, Navy, Pastel, Earth
- Image: 프리셋 이미지 20개 (피아노/바이올린)

미리보기:
- 600x350px 크기
- 변경 사항 실시간 반영
- 모바일 반응형
```

### UX 개선 프롬프트
```
다음을 개선해줘:

1. 명함 저장 시 토스트 알림 표시
2. My Saved Cards를 2열 그리드로 변경
3. 저장된 명함에서 실제 명함 디자인 미리보기
4. 이미지가 잘리지 않도록 크기 조정
5. Load 버튼을 Edit으로 변경하고 아이콘 추가
```

## 🎯 성공적인 협업 팁

1. **명확한 의사소통**: 원하는 것을 구체적으로 표현
2. **단계별 검증**: 각 단계의 결과를 확인
3. **유연성 유지**: 더 나은 방법이 제안되면 열린 마음으로 수용
4. **문서화**: 중요한 결정 사항은 문서로 기록
5. **점진적 개선**: 완벽을 추구하기보다 작동하는 것부터 만들고 개선

이 가이드를 참고하여 Claude Code와 효과적으로 협업하면
복잡한 웹 애플리케이션도 체계적으로 만들 수 있습니다!
